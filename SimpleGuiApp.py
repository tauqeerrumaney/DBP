
#!/usr/bin/python3
import tkinter as tk
import os, sys
root = tk.Tk() # toplevel
root.title('DGApp')
w = tk.Label(root, text='Hello, world!')
w.pack(fill='both',expand=True,padx=5)
# root.mainloop() # app is ready
